年前最一次更新。对程序进行了修改和优化，增加了矢量绘图，配合36MHz的晶振，最高采样频率达到200KHz，即5us一次。
 一点经验：在设置变量是考虑好变量的取值范围，设置合适的变量类型。程序中有一个100字节的数组，开始设置的是int型，但它只是存储adc结果，取值范围由0——255，改成无符号的char型后，占用内存明显减少。
 一点问题：图中采用的是36MHz的晶振，在网上查知有基频晶振和泛音晶振两种，而且泛音晶振基本上是奇数次，但这个36MHz晶振在检测mcu时显示为18MHz，但用的时候实际工作在36MHz，不知道为什么？